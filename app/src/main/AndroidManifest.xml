<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.power_upblackjack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_red_joker_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_red_joker_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PowerUpBlackjack">
        <activity android:name=".AppInfo"></activity>
        <activity android:name=".HowToPlay" />
        <activity android:name=".Options" />
        <activity android:name=".GameScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>